apiVersion: v1
kind: Secret
metadata:
  name: upbound-bootstrap-config
  labels:
    {{- include "labelsBootstrapper" . | nindent 4 }}
type: Opaque
{{- if and .Values.upbound.controlPlane.connect }}
data:
  cpTokenSecretName: {{ .Values.upbound.controlPlane.tokenSecret | b64enc | quote }}
{{- end }}
{{- if .Values.upbound.controlPlane.connect }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "graphql-name" . }}
  labels:
    {{- include "labelsGraphql" . | nindent 4 }}
{{- end }}